#图  
用哪一个数据结构来表示图 
要求  
1. 它必须为可能在应用中碰到的各种类型的图预留出足够的空间  
2. Graph的实例方法的实现一定要快  

解决方案  
邻接表数组:可以使用一个以顶点为索引的列表数组，其中的每个元素都是和该顶点相邻的顶点列表。  

这里使用Bag抽象数据类型来实现这个链表，就可以在常数时间内添加新的边或遍历任意顶点的所有相邻顶点。    

Graph的实现使用了一个由顶点索引的整型链表数组，每条边都会出现两次。