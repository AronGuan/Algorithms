#基于线性探测法的散列表  
思路:用大小为M的数组保有N个键值对，其中PM>N,需要依靠数组中的空位解决碰撞冲突。  

线性探测:当碰撞发生时(当一个键的散列值已经被另一个不同的键占用)，直接检查散列表中的下一个位置(将索引值加1)  

实现中使用并行数组，一条保存键，一条保存值，使用散列函数产生访问数组所需的数组索引。  

删除操作:需要将簇中被删除键的右侧的所有键重新插入散列表。  

缺点:当散列表快满的时候查找所需的探测次数时巨大的。需要调整数组  

散列表支持和数组大小无关的常数级别的查找和输入操作。