#堆排序  
思路:先构造出二叉堆，然后再排序  

1.用sink()构造堆，可以跳过大小为1的子堆，从右向左用sink()，最后形成一个堆有序的数组。  

2.然后循环把最大的元素a[1]和a[N]进行交换并修复堆(通过sink()方法)  

堆排序是唯一能够同时最优的利用空间和时间的方法，在资源比较紧张的时候，比如低成本的移动资源上很流行，但是它无法利用缓存，即数组元素很少和相邻的其他元素进行比较，所以缓存未命中的次数要远远高于其他比较都在相邻元素间进行的算法。因此在许多应用中很少使用它。